# CandidateAI - Hybrid Architecture Requirements
# Optimized for laptop deployment with quantized models

# Core dependencies
requests>=2.31.0          # HTTP requests
python-dotenv>=1.0.0     # Environment variables
structlog>=23.2.0        # Logging
psutil>=5.9.0            # Memory checking

# PDF processing
pymupdf>=1.23.0         # PDF text extraction

# Local LLM and quantization
# Install Oumi for quantized models (optional)
# pip install oumi>=0.8.0 bitsandbytes accelerate

# Or use Ollama for lightweight models
# Download from https://ollama.ai/download

# Production dependencies (uncomment for web interface)
# fastapi>=0.104.0
# uvicorn>=0.24.0
# pydantic>=2.5.0
# redis>=5.0.0

# Development dependencies (uncomment for development)
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0

# Optional quantization support (uncomment if using GPU)
# torch>=2.0.0
# transformers>=4.30.0
# bitsandbytes>=0.41.0
# accelerate>=0.20.0

# Memory monitoring and optimization
# pip install nvidia-ml-py3  # For GPU memory monitoring
# pip install GPUtil        # For GPU usage monitoring